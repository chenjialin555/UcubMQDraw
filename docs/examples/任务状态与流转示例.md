# 任务状态与流转示例

> 设计说明见 [任务状态与任务流转.md](../details/任务状态与任务流转.md)。状态枚举见 [API接口.md](../API接口.md#七任务状态枚举)。

## 一、状态与前端展示映射

```ts
// frontend/src/utils/taskStatus.ts

export const STATUS_LABEL: Record<string, string> = {
  created: '已创建',
  queued: '排队中',
  dispatching: '准备调度',
  running: '生成中',
  completed: '已完成',
  failed: '失败',
}

export const RUNNING_STATUSES = [
  'created',
  'queued',
  'dispatching',
  'running',
]

export function isRunning(status: string): boolean {
  return RUNNING_STATUSES.includes(status)
}
```

## 二、TaskCard.vue 示例

```vue
<template>
  <div class="task-card">
    <div class="previews">
      <img :src="inputUrl || placeholderUrl" class="thumb" />
      <img v-if="outputUrl" :src="outputUrl" class="thumb" />
    </div>

    <div class="meta">
      <span>{{ task.toolName }}</span>
      <span>{{ STATUS_LABEL[task.status] || task.status }}</span>
    </div>

    <el-progress
      v-if="isRunning(task.status)"
      :percentage="task.progress"
      :stroke-width="6"
    />

    <p v-if="task.status === 'failed'" class="error">
      {{ task.errorMessage }}
    </p>
  </div>
</template>

<script setup lang="ts">
import { computed } from 'vue'
import type { GenerationTask } from '@/types/task'
import { STATUS_LABEL, isRunning } from '@/utils/taskStatus'

const props = defineProps<{ task: GenerationTask }>()

const placeholderUrl = '/placeholder.png'

const inputUrl = computed(() => {
  const list = props.task.taskSubmitParams?.refImageList
  return Array.isArray(list) && list[0] ? String(list[0]) : ''
})

const outputUrl = computed(() => {
  const list = props.task.taskCallbackParams?.imageUrlList
  return Array.isArray(list) && list[0] ? String(list[0]) : ''
})
</script>
```

## 三、taskStore 筛选「进行中」

```ts
getters: {
  filteredTasks(state) {
    if (state.currentFilter === '进行中') {
      return state.tasks.filter(t => RUNNING_STATUSES.includes(t.status))
    }
    if (state.currentFilter === '已完成') {
      return state.tasks.filter(t => t.status === 'completed')
    }
    return state.tasks
  },
},
```

## 四、后端状态枚举

```python
# backend/app/models/task_model.py
from enum import Enum


class TaskStatus(str, Enum):
    CREATED = "created"
    QUEUED = "queued"
    DISPATCHING = "dispatching"
    RUNNING = "running"
    COMPLETED = "completed"
    FAILED = "failed"
```

## 五、normalize_status（回调用）

```python
def normalize_status(raw: str | None) -> str:
    if raw in ("success", "completed"):
        return "completed"
    if raw in ("failed", "error"):
        return "failed"
    return raw or "failed"
```

## 六、create_task 完整流转（伪代码）

```python
def create_task(data: dict):
    tool = get_tool(data["toolKey"])
    params = data.get("params") or {}
    tool.validate(params)

    task_id = generate_task_id()
    inner_task_id = generate_inner_task_id()
    dispatch = tool.build_task_dispatch_params(task_id, inner_task_id, params)

    task = save_task(
        task_id=task_id,
        inner_task_id=inner_task_id,
        status="created",
        progress=0,
        task_submit_params=params,
        task_dispatch_params=dispatch,
    )

    if settings.USE_MOCK:
        update_status(task_id, "queued", 5)
        start_mock_task(task_id)
    else:
        mq_service.send(dispatch, tool_key=tool.key)
        update_status(task_id, "queued", 5)

    task = get_task(task_id)
    websocket_manager.broadcast_task_update(task)
    return task
```

## 七、Mock 模式状态步进

```python
# mock_task_runner.py
steps = [
    ("queued", 5),
    ("dispatching", 15),
    ("running", 30),
    ("running", 50),
    ("running", 75),
    ("running", 90),
    ("completed", 100),
]

for status, progress in steps:
    time.sleep(1)
    task = update_status(task_id, status, progress)
    if status == "completed":
        task = update_result(
            task_id=task_id,
            task_callback_params={
                "status": "success",
                "imageUrlList": ["http://object.intuly.com/.../mock-out.png"],
                "costTime": 6.5,
            },
            cost_time=6.5,
        )
    websocket_manager.broadcast_task_update(task)
```

## 八、真实 MQ 回调后更新

```python
def handle_imggen_callback(message: dict):
    inner_task_id = message["innerTaskId"]
    task = get_by_inner_task_id(inner_task_id)

    updated = update_result(
        task_id=task.task_id,
        status=normalize_status(message.get("status")),
        progress=100,
        task_callback_params=message,
        error_message=message.get("errorMsg") or "",
        cost_time=message.get("costTime"),
    )
    websocket_manager.broadcast_task_update(updated)
```

## 九、时间线示意

```text
T+0s   POST /tasks        status=created  → WS 推送
T+0s   入队               status=queued   progress=5
T+1s   Mock/MQ            status=dispatching progress=15
T+2s   imggen 执行        status=running  progress=30…90
T+7s   回调/Mock 完成     status=completed progress=100
       taskCallbackParams.imageUrlList 有值 → 右栏显示输出图
```

## 十、相关示例

- Mock 细节：[Mock模式设计.md](../details/Mock模式设计.md)
- MQ 回调：[RocketMQ对接示例.md](./RocketMQ对接示例.md)
- WS 推送：[WebSocket推送示例.md](./WebSocket推送示例.md)
